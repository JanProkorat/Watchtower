export { mergeAttention } from './mergeAttention.js';
export type { BellItem } from './mergeAttention.js';

// Bell/thread UI components (bell icon, thread panel, reply composer) land
// here in D2/D3 — this package currently only exports the merge/de-dupe
// helper consumed by the iOS apps' attention feed.
