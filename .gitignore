# Dependencies
node_modules/

# Build outputs
dist-electron/
dist-orchestrator/
dist-renderer/
dist-helper/
dist-shared/
dist-helper-src/
release/
packages/shared/dist/
*.tsbuildinfo

# Test / coverage
coverage/

# Local databases
data.db*

# Stray compiled output if tsc ever runs with the wrong outDir
shared/*.js
shared/*.js.map

# Logs and misc runtime
*.log

# OS
.DS_Store

# Editors
.vscode/
.idea/

# Env / secrets
.env
.env.*
!.env.example

# Brainstorming visual companion (mockups during design sessions)
.superpowers/

# Local Claude Code scratch (worktrees, transient state)
.claude/

# Capacitor build outputs
apps/ipad/dist/
apps/ipad/ios/App/App/public/
apps/iphone/dist/
apps/iphone/ios/App/App/public/

# WatchtowerCore is a path-referenced library; its lockfile is re-churned by
# the app xcodegen/build. Each app pins via its own .xcodeproj Package.resolved.
swift/WatchtowerCore/Package.resolved
