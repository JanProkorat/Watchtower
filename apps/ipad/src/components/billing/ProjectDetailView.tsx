// ProjectDetailView — stub; fleshed out in Task 11.
export function ProjectDetailView({ projectId, onBack }: { projectId: number; onBack: () => void }): JSX.Element {
  void projectId; // prop reserved for Task 11
  void onBack;    // prop reserved for Task 11
  return <div style={{ padding: 20, color: '#8B88A6' }}>Detail projektu (R1)</div>;
}
