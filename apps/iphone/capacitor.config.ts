import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'cz.greencode.watchtower.iphone',
  appName: 'Watchtower',
  webDir: 'dist',
};

export default config;
